<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="top-bar">
      <div class="container">
        <a class="logo" href="/">Question</a>||
        <a class="new" href="/questions/new">New</a>
        <ul class="large-3 column right inline-list" style="margin-top: 10px">
          <% if signed_in? %>
            <li><span style="color: blue">Signed in as<span></li>
            <li>
              <img style="border-radius: 30px; width:35px; margin-top: -3px; postion: relative" src="<%= current_user.avatar_url %>">
            </li>
            <li><a href="/sign_out">Sign out</a></li>
          <% else %>
            <li><a href="/auth/facebook">Sign in</a></li>
          <% end %>
        </ul>
      </div>
    </div>
    <% if flash.any? %>
      <div class="flash">
        <div class="container">
          <%= flash[:notice] %>
        </div>
      </div>
    <% end %>
    <div class="container">
      <%= yield %>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
